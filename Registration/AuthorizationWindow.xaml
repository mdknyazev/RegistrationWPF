<Window x:Class="Registration.AuthorizationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Registration"
        mc:Ignorable="d"
        Title="AuthorizationWindow" Height="200" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock>Логин</TextBlock>
        <TextBlock Grid.Row="1">Пароль</TextBlock>
        <TextBox x:Name="login" Grid.Column="1" Grid.Row="0"></TextBox>
        <PasswordBox x:Name="password" Grid.Column="1" Grid.Row="1"></PasswordBox>
        <Button x:Name="AuthBtn" Grid.Row="2" Grid.Column="0" Click="AuthBtn_Click">Войти</Button>
        <Button x:Name="RegistrationBtn" Grid.Row="2" Grid.Column="1" Click="RegistrationBtn_Click">Зарегестрироваться</Button>
    </Grid>
</Window>
